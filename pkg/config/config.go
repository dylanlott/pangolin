package main

import "fmt"

// Create looks for a config and creates one if it doesnt exist
func Create() {
	panic("not impl")
}

// Load loads configs into the system
func Load() {
	panic("not impl")
}
